<div class="row g-3">
  <div class="col-md-6 d-flex flex-column justify-content-center">
    <h2 class="fw-bold">Quiénes somos</h2>
    <p>Somos Happy Plant, un sistema diseñado para automatizar y monitorear el cuidado de tus plantas.</p>

    <% if(plantas && plantas.length > 0) { %>
      <h5 class="mt-3">Plantas registradas:</h5>
      <ul>
        <% plantas.forEach(p => { %>
          <li><strong><%= p.nombre %></strong> - Ubicación: <%= p.ubicacion %></li>
        <% }) %>
      </ul>
    <% } %>
  </div>

  <div class="col-md-6 text-center">
    <div class="bg-light border rounded" style="height: 250px;">
      Imagen
    </div>
  </div>
</div>
